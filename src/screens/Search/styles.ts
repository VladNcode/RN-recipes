import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginLeft: -24,
    flex: 1,
  },
  list: {
    marginBottom: 48,
  },
  input: { width: '80%', marginLeft: 24 },
});
